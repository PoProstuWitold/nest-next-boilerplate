import * as path from 'path';
import { Module } from '@nestjs/common';
import { MailerModule } from '@nestjs-modules/mailer';
import { HandlebarsAdapter } from '@nestjs-modules/mailer/dist/adapters/handlebars.adapter';

@Module({
    imports: [
        MailerModule.forRoot({
            transport: {
                host: 'smtp.mailtrap.io',
                port: 2525,
                auth: {
                    user: '70f3e71fb0c602', //generated by Mailtrap
                    pass: '7f3233720db83d' //generated by Mailtrap
                }
            },
            defaults: {
                from: '"PoProstuWitold" <no-reply@poprostuwitold.com>', // outgoing email ID
            },
            preview: true,
            template: {
                dir: path.join(__dirname, 'templates/pages'),
                adapter: new HandlebarsAdapter(),
                options: {
                    strict: true
                }
            },
            options: {
                partials: {
                    dir: path.join(__dirname, 'templates/partials'),
                    options: {
                        strict: true
                    }
                }
            }
        }),
    ]
})
export class MailModule {}